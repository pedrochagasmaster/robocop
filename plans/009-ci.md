# Plan 009: Add CI (GitHub Actions) running compile, lint, typecheck, and tests

> **Executor instructions**: Follow this plan step by step. Run every
> verification command and confirm the expected result before moving to the
> next step. If anything in the "STOP conditions" section occurs, stop and
> report — do not improvise. When done, update the status row for this plan
> in `plans/README.md` — unless a reviewer dispatched you and told you they
> maintain the index.
>
> **Drift check (run first)**: `git diff --stat 8b4241e..HEAD -- .github pyproject.toml AGENTS.md`
> If a `.github/workflows/` directory now exists, STOP — CI was added
> independently; reconcile rather than duplicate.

## Status

- **Priority**: P2
- **Effort**: M
- **Risk**: MED
- **Depends on**: plans/008-lint-typecheck-gates.md (CI runs the gates 008 adds)
- **Category**: dx
- **Planned at**: commit `8b4241e`, 2026-06-27

## Why this matters

There is no CI on either remote (`.github/workflows/`, `bitbucket-pipelines.yml`
both absent). Regressions land undetected before Bitbucket snapshot publish or
edge deploy. The repo has two remotes (`origin` = GitHub for issues, `bitbucket`
= corporate writable remote per `AGENTS.md:121-123`); GitHub Actions is the
reachable gate from a Cursor Cloud/dev context and covers PRs pushed to
`origin`. This plan adds a single GitHub Actions workflow that runs
compileall, ruff, mypy, and pytest on push and PR, so a regression is caught
before it reaches either remote.

## Current state

Repo-wide glob confirms no `.github/` directory, no `bitbucket-pipelines.yml`,
no `Jenkinsfile`, no `.gitlab-ci.yml`.

`AGENTS.md:121-123` — documents dual remotes with no automated gate.

`pyproject.toml:15-19` — `dev` extra has `pytest`, `pytest-asyncio` (and
`ruff`, `mypy` after Plan 008).

`tests/conftest.py` — the `mock_env` fixture sets `DISPATCH_DATA_ROOT`,
`DISPATCH_SCR_DIR`, `DISPATCH_MOCK_SCENARIO`, etc. per test. The suite is
self-contained and does not require a real edge node.

**Repo conventions**: no existing CI to match. GitHub Actions is the natural
choice for `origin`. The workflow lives at `.github/workflows/ci.yml`. Use
the standard `ubuntu-latest` runner with Python 3.10 (the floor) — or a
matrix of 3.10/3.12 to catch version-specific issues. Keep it minimal.

## Commands you will need

| Purpose   | Command                          | Expected on success |
|-----------|----------------------------------|---------------------|
| Validate YAML | `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` | exit 0 |

(The workflow itself runs on GitHub; local validation is YAML syntax only.)

## Scope

**In scope**:
- `.github/workflows/ci.yml` (create the `.github/workflows/` directory tree)
- `AGENTS.md` — note that CI runs on `origin` PRs (one line in the Validation
  or remotes section)

**Out of scope**:
- `bitbucket-pipelines.yml` — the corporate remote's CI is a separate,
  environment-specific setup. File it independently if the team wants a
  Bitbucket-side gate. This plan only adds the GitHub gate.
- Pre-commit hooks — separate DX plan; this is CI-only.
- Making tests pass on Windows in CI — the runner cancel test is
  `skipif(os.name == "nt")`; on `ubuntu-latest` it runs. Do NOT change the
  skip in this plan.

## Git workflow

- Branch: `advisor/009-ci`
- Commit per step; message style: `chore(dx): add GitHub Actions CI workflow`
- Do NOT push or open a PR unless the operator instructed it.

## Steps

### Step 1: Create the workflow file

Create `.github/workflows/ci.yml`:

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dev dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
      - name: Compile
        run: python -m compileall dispatch scr
      - name: Lint
        run: ruff check dispatch tests
      - name: Format check
        run: ruff format --check dispatch tests
      - name: Typecheck
        run: mypy dispatch
      - name: Help smoke
        run: python -m dispatch --help
      - name: Tests
        run: python -m pytest tests tools/prod_tui/tests -q
```

If Plan 008 has not landed yet, the `Lint`/`Format check`/`Typecheck` steps
will fail because `ruff`/`mypy` are not installed and not configured. This
plan **depends on 008** — do not run 009 before 008.

### Step 2: Note CI in AGENTS.md

In `AGENTS.md` under the remotes or Validation section, add one line:

```markdown
CI (GitHub Actions) runs compile, lint, typecheck, and tests on every push to
`main` and on PRs to `origin`. See `.github/workflows/ci.yml`.
```

**Verify**: `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` → exit 0.

## Test plan

- No new tests — the workflow IS the test. Local verification is YAML syntax.
- If a `act` tool (local Actions runner) is available, optionally run
  `act -j test` to smoke-test the workflow locally; otherwise rely on the
  first push to `origin` to validate it.

## Done criteria

- [ ] `.github/workflows/ci.yml` exists and is valid YAML
- [ ] `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0
- [ ] `grep -n "GitHub Actions" AGENTS.md` returns a match
- [ ] The workflow references `ruff check`, `ruff format --check`, `mypy`,
      and `pytest` (all four gates present)
- [ ] No files outside `.github/` and `AGENTS.md` are modified (`git status`)
- [ ] `plans/README.md` status row updated

## STOP conditions

Stop and report back (do not improvise) if:
- `.github/workflows/` already exists (CI was added independently — reconcile
  by merging the gate steps into the existing workflow rather than creating a
  second one).
- Plan 008 has not landed (ruff/mypy are not in the `dev` extra and not
  configured) — STOP; this plan cannot pass CI without 008.
- `pip install -e ".[dev]"` fails on `ubuntu-latest` Python 3.10 because a
  transitive dep dropped 3.10 support — STOP and report; the matrix may need
  to drop 3.10 or the dep needs pinning.

## Maintenance notes

- The matrix runs 3.10 (the floor) and 3.12 (the cloud venv). If a future
  `requires-python` bump drops 3.10, remove it from the matrix.
- The `tools/prod_tui/tests` suite is included in the pytest step. If that
  suite requires tmux/ssh fixtures unavailable on `ubuntu-latest`, it will
  fail — in that case, split it into a separate job or exclude it and note
  why in a follow-up.
- Bitbucket-side CI is intentionally out of scope; file a separate plan if
  the team wants a mirrored `bitbucket-pipelines.yml`.
- Reviewer: confirm the workflow runs on PRs (not just `main` push) so
  contributor branches get gated before merge.
