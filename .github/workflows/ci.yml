name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install uv
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          version: "0.12.0"
      - name: Install dev dependencies
        run: uv sync --locked --extra dev --python ${{ matrix.python-version }}
      - name: Compile
        run: uv run --locked --no-sync python -m compileall dispatch scr
      - name: Lint
        run: uv run --locked --no-sync ruff check dispatch tests
      - name: Lint scr
        run: uv run --locked --no-sync ruff check scr
      - name: Format check
        run: uv run --locked --no-sync ruff format --check dispatch tests
      - name: Typecheck
        run: uv run --locked --no-sync mypy dispatch/sql.py dispatch/jobs.py dispatch/manifest.py
      - name: Help smoke
        run: uv run --locked --no-sync python -m dispatch --help
      - name: Tests
        run: uv run --locked --no-sync python -m pytest

  windows-test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install uv
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          version: "0.12.0"
      - name: Install dev dependencies
        run: uv sync --locked --extra dev --python 3.12
      - name: Bind py to the uv project interpreter
        shell: powershell
        run: |
          $pythonExe = Join-Path $PWD ".venv\Scripts\python.exe"
          if (-not (Test-Path -LiteralPath $pythonExe -PathType Leaf)) {
            throw "uv project interpreter not found at $pythonExe"
          }
          $shimDir = Join-Path $env:RUNNER_TEMP "uv-project-py-shim"
          $shimPath = Join-Path $shimDir "py.cmd"
          New-Item -ItemType Directory -Path $shimDir -Force | Out-Null
          [System.IO.File]::WriteAllLines(
            $shimPath,
            @("@echo off", "`"$pythonExe`" %*"),
            [System.Text.Encoding]::ASCII
          )
          Add-Content -Path $env:GITHUB_PATH -Value $shimDir
          & $shimPath -c "import sys; assert sys.version_info[:2] == (3, 12), sys.version; print(sys.executable); print(sys.version)"
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
      - name: Authoritative Windows local check
        run: powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\dev\local_check.ps1
      - name: Focused Windows xdist regressions
        run: >-
          py -m pytest
          tests/test_capacity.py::test_launch_intents_are_admitted_fifo_across_processes
          tests/test_capacity.py::test_dead_launch_intent_is_reclaimed
          tests/test_process.py::test_cancelled_impala_query_reaps_child_before_releasing_capacity
          tests/test_process.py::test_run_exec_timeout_awaits_subprocess_cleanup
          tests/test_process.py::test_windows_terminate_and_reap_stops_real_child
          tests/test_new_job_queue.py::test_cancelled_launch_removes_shared_intent
          -n 4 --dist loadfile -q
